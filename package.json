{
  "name": "sptribs-e2etests",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.16.0"
  },
  "scripts": {
    "configure": "yarn playwright install && yarn playwright install webkit && yarn playwright install firefox && yarn playwright install msedge",
    "test:ui": "npx playwright test --project chromium --headed",
    "test:fullfunctional": "yarn playwright install && npx playwright test --project chromium",
    "test:crossbrowser": "yarn playwright install && yarn playwright install webkit && yarn playwright install firefox && echo 'Install successful' && npx playwright test --project webkit --project firefox",
    "test:ChromeDSS": "yarn playwright install && npx playwright test --project chromium --grep @DSSCreate",
    "test:FirefoxDSS": "yarn playwright install firefox && echo 'Install successful' && npx playwright test --project firefox --grep @DSSCreate",
    "test:WebkitDSS": "yarn playwright install webkit && echo 'Install successful' && npx playwright test --project webkit --grep @DSSCreate",
    "test:CrossDeviceDSS": "yarn playwright install && yarn playwright install webkit && yarn playwright install firefox && echo 'Install successful' && npx playwright test --project MobileSafari --project MobileChrome --grep @DSSCreate",
    "test:AccessibilityDSS": "yarn playwright install && npx playwright test --project chromium --grep @DSSAccessibility",
    "test:ChromeCaseAPI": "yarn playwright install && npx playwright test --project chromium --grep @CaseAPI",
    "test:FirefoxCaseAPI": "yarn playwright install firefox && echo 'Install successful' && npx playwright test --project firefox --grep @crossbrowserCaseAPI",
    "test:WebkitCaseAPI": "yarn playwright install webkit && echo 'Install successful' && npx playwright test --project webkit --grep @crossbrowserCaseAPI",
    "test:CrossDeviceCaseAPI": "yarn playwright install && yarn playwright install webkit && yarn playwright install firefox && echo 'Install successful' && npx playwright test --project MobileSafari --project MobileChrome --grep @CaseAPI",
    "test:AccessibilityCaseAPI": "yarn playwright install && npx playwright test --project chromium --grep @accessibilityCaseAPI",
    "test:ChromeUC": "yarn playwright install && npx playwright test --project chromium --grep @DSSUpdate",
    "test:FirefoxUC": "yarn playwright install firefox && echo 'Install successful' && npx playwright test --project firefox --grep @DSSUpdate",
    "test:WebkitUC": "yarn playwright install webkit && echo 'Install successful' && npx playwright test --project webkit --grep @DSSUpdate",
    "test:CrossDeviceUC": "yarn playwright install && yarn playwright install webkit && yarn playwright install firefox && echo 'Install successful' && npx playwright test --project MobileSafari --project MobileChrome --grep @DSSUpdate",
    "test:AccessibilityUC": "yarn playwright install && npx playwright test --project chromium --grep @UpdateAccessibility",
    "test:report": "yarn playwright show-report",
    "test:accessibility": "npx playwright test --grep @accessibility --project chromium",
    "lint": "yarn prettier . --write",
    "delete-branch": "ts-node src/tests/helpers/nightlyDevDeletion.ts",
    "test:dataCleanUp": "yarn playwright install && npx playwright test --project chromium --grep @dataCleanup",
    "test:ChromeCaseAPI1": "yarn playwright install && npx playwright test --project chromium --grep @CaseAPI1",
    "test:ChromeCaseAPI2": "yarn playwright install && npx playwright test --project chromium --grep @CaseAPI2",
    "test:ChromeCaseAPI3": "yarn playwright install && npx playwright test --project chromium --grep @CaseAPI3",
    "test:ChromeCaseAPI4": "yarn playwright install && npx playwright test --project chromium --grep @CaseAPI4",
    "test:ChromeCaseAPI5": "yarn playwright install && npx playwright test --project chromium --grep @CaseAPI5",
    "test:ChromeCaseAPI6": "yarn playwright install && npx playwright test --project chromium --grep @CaseAPI6",
    "test:ChromeCaseAPI7": "yarn playwright install && npx playwright test --project chromium --grep @CaseAPI7"
  },
  "pre-commit": [
    "lint"
  ],
  "dependencies": {
    "@axe-core/playwright": "^4.10.0",
    "axios": "^1.7.5",
    "dependency-check": "^4.1.0",
    "playwright-core": "^1.46.1",
    "pre-commit": "^1.2.2",
    "typescript": "^5.5.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.1",
    "@types/node": "^20.10.0",
    "prettier": "3.3.3",
    "ts-node": "^10.9.2"
  },
  "packageManager": "yarn@4.5.1"
}
