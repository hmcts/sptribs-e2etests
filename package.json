{
  "name": "testProject",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.16.0"
  },
  "scripts": {
    "configure": "yarn playwright install && yarn playwright install webkit && yarn playwright install firefox && yarn playwright install msedge",
    "test:ui": "npx playwright test --project chromium --headed --reporter=dot",
    "test": "npx playwright test --project chromium --reporter=dot",
    "test:crossbrowser": "npx playwright test --project webkit --project firefox --project MobileSafari --project MicrosoftEdge --reporter=dot",
    "test:report": "yarn playwright show-report"
  },
  "dependencies": {
    "@axe-core/playwright": "^4.8.1",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0"
  }
}
